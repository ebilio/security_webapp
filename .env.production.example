# Production Environment Variables
# Copia questo file in backend/.env per produzione locale
# Su Render, configura queste variabili nel Dashboard

# Server
NODE_ENV=production
PORT=10000

# CORS - Aggiorna con il tuo URL Render dopo il deploy
CORS_ORIGIN=https://security-webapp-xxxx.onrender.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CVE API (opzionale)
# Ottieni una chiave gratuita su: https://nvd.nist.gov/developers/request-an-api-key
NVD_API_KEY=
