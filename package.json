{
  "name": "security-webapp",
  "version": "2.0.0",
  "description": "Client-side web application per valutazione sicurezza browser",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "keywords": ["security", "browser", "assessment", "fingerprint", "client-side"],
  "author": "",
  "license": "MIT"
}
